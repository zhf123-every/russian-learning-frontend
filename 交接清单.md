# 俄语学习前端项目 · 最新交接清单
更新时间：2026-09-22（含骨架重构）

## 项目信息
- 前端：C:\Users\张宏飞\Desktop\website_source\russian-learning-frontend-main
- 后端：C:\Users\张宏飞\russian-learning（server.py）
- GitHub：https://github.com/zhf123-every/russian-learning-frontend.git（main→Cloudflare Pages）
- 线上：https://russian-learning-frontend.pages.dev
- 后端API：https://russian-learning-jetq.onrender.com

## 当前技术栈
React + Vite + Tailwind v4 + DaisyUI（light主题，紫色主色 #6d28d9）
全局字体：html font-size: 22px
（依赖仅 React/react-dom/react-router-dom/zustand/ts-fsrs + tailwind/daisyui，无新增依赖）

## 当前布局结构（对标 julebu.co 应用骨架重构，2026-09-22）
```
顶部通栏（60px，白背景，sticky z-30）
├─ 左侧：收起按钮 + А俄语学习logo
└─ 右侧：体验会员 + 金币 + 钻石 + 头像

下面 .shell-body（flex row，relative）—— 骨架三件套：
├─ ① 侧边栏占位符 .shell-sidebar-ph（300px，static 占文档流，防遮挡；≤900px隐藏）
├─ ② 侧边栏固定实体 .db-sidenav（300px，position:fixed top:60 left:0 bottom:0 z-20）
│  └─ 菜单：主页/解锁游戏/我的游戏/即将上线项... + 底部今日目标
└─ ③ 主内容区 .shell-main（flex:1，min-width:0，overflow-y:auto，#f5f5f5，padding:16px）
   └─ 白色大卡片容器 .shell-card（圆角16px，阴影，min-height:100%）
      ├─ 内容页页眉 .shell-card-header（56px，◧收起按钮 + 收起侧边栏 + 右按钮组，底部1px线）
      └─ <Outlet />（主页为嵌套网格，见下）

主页内容区（对标 julebu.co 嵌套网格，2026-09-22）：
.home-grid（外层 grid：1fr + 250px 右栏，gap 24px）
├─ .home-main（主区 grid：1fr 1fr 左+中两列，gap 16px）
│  ├─ 左列：每日打卡 + 重复通关
│  ├─ 中列：每日任务 + 陌生关卡
│  └─ .col-entry：我的游戏入口（跨左+中两列）
└─ .right-col（右栏 250px，锁第2列，跨两行）
   ├─ 六格掌握度（雷达图） + 最近学习 + 邀请有礼
```
## 关键文件
- src/components/layout/AppShell.jsx — 布局外壳（骨架三件套：占位符+fixed侧栏+flex-1主内容）
- src/components/layout/TopBar.jsx — 顶部通栏
- src/components/layout/SideNav.jsx — 侧边栏（fixed 实体）
- src/components/layout/MobileNav.jsx — 移动端顶栏+抽屉+底部Tab
- src/pages/Dashboard.jsx — 主页（左+中+右栏嵌套网格，卡片逻辑未动）
- src/styles/tailwind.css — light主题，html font-size:22px
- src/styles/dashboard.css — .app-shell/.shell-*/骨架样式（22-77行）+ .db-sidenav fixed（76行起）+ .home-grid嵌套网格（480-513行）

## 骨架重构要点（对标 julebu.co）
- 侧边栏从 sticky 改为 fixed：.db-sidenav { position:fixed; top:60px; left:0; bottom:0; z-index:20 }
- 新增占位符 .shell-sidebar-ph：桌面端占文档流宽度，防 fixed 侧栏遮挡内容；折叠时与侧栏同步 width:0
- 主内容区 .shell-main 保持 flex:1，折叠后自动扩展到全宽
- 折叠：.app-shell.shell-collapsed → 占位符和侧栏宽度过渡到 0
- 移动端 ≤900px 不变：占位符隐藏、侧栏隐藏，仍用旧抽屉+底部Tab


## 嵌套网格平移要点（对标 julebu.co 主页，2026-09-22）
- .home-grid 从「三列并排」改为「嵌套」：外层 1fr + 右栏250px（gap 24px）
- 新增 .home-main：左+中两列 1fr 1fr（gap 16px），容纳左列/中列/col-entry
- .right-col 锁第2列跨两行（原第3列）；.col-entry 跨主区左中两列（原跨整个网格）
- 右栏宽度/颜色参数仍在 .home-grid 顶部 --rc-* 变量区调整
- 窄屏 ≤1023px 回落单列；移动端 ≤900px 维持旧抽屉+底部Tab

## Tailwind Play 文件（用户自己调参用）
- tailwind-play-主页.html — 完整HTML（顶栏+侧边栏+内容页页眉+三栏卡片，全部类名化）
- tailwind-play-主页.css — CSS面板，所有★参数（导航栏/侧栏/页眉/卡片）
- 备份：backups/play-2026-09-22/（旧版Play）、backups/skeleton-refactor-2026-09-22/（AppShell+dashboard.css旧版）

## 用户当前在做什么
1. 在 Tailwind Play 里自己调布局参数（侧边栏宽度、页眉高度、卡片大小等），调好后告诉 assistant 再同步线上
2. 2026-09-22 已按 julebu.co 骨架重构 AppShell（占位符+fixed侧栏+flex-1主内容）

## 重要约束
- ≤900px移动端保持旧抽屉+底部Tab
- main分支推送自动部署到Cloudflare Pages
- 部署后需1-2分钟+浏览器强刷(Ctrl+Shift+R)
- 单元测试courseStore.test.js有5个历史失败，不阻断部署（本次构建通过，未新增失败）
- 每次重要修改建备份文件夹
